import 'package:flutter/material.dart';

/// =====================================================================
/// CLASSE AppColors
/// Centraliza a paleta de cores oficial do aplicativo.
/// Evita o uso de códigos hexadecimais espalhados pelo projeto, garantindo
/// consistência visual e facilitando alterações globais de tema.
/// =====================================================================

class AppColors {
  // LARANJA
  static const Color orange = Color(0xFFFF5D28);

  // AZUL ACINZENTADO
  static const Color blueGrey = Color(0xFF607999);

  // AZUL CLARO
  static const Color lightBlue = Color(0xFFD9F3FF);

  // AZUL MARINHO
  static const Color darkBlue = Color(0xFF0D4A75);
}
